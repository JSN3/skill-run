class SkillDemoPractice {
    public static boolean greaterThan(int bigger, int smaller) {
        if (bigger > smaller) {
            return true;
        }
        else if (bigger < smaller) {
            return false;
        }
        else {
            return false;
        }
    }
}
